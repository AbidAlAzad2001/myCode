#include<stdio.h>
#include<string.h>
int main()
{
    int t;
    int n;
    scanf("%d",&t);
    while(t--){
        scanf("%d",&n);
        char str[n+1];
        scanf("%s",str);
    }

    return 0;
}
