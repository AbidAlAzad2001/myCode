#include<stdio.h>
int main()
{
    int t,n,a[101];
    scanf("%d",&t);
    while(t--){
        scanf("%d",&n);
        while(n--){
            scanf("%d",&a[n]);
        }
    }

    return 0;
}
